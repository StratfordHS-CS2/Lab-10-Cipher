/**
 * You will implement a reverse cipher.  One parameter is necessary for encoding or decoding.
 *
 * String msg - this is the message to be encoded or decoded
 *
 * You will be reversing the message.
 *
 * Usage:
 * System.out.println( Reverse.encode( "My Message" ) ); // outputs "EGASSEMYM"
 * System.out.println( Reverse.decode( "EGASSEMYM" ) ); // outputs "MYMESSAGE"
 *
 * You should make every letter uppercase and remove spaces.
 */
public class Reverse
{
    public String encode( String msg )
    {
        
    }

    public String decode( String msg )
    {
        
    }
}
