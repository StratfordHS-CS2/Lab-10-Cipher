/**
 * You will implement a rotation cipher.  Two parameters are necessary for encoding or decoding.
 *
 * String msg - this is the message to be encoded or decoded
 * int rot - this is how far to rotate the alphabet
 *
 * Reference:
 * https://en.wikipedia.org/wiki/Caesar_cipher
 *
 * Usage:
 * System.out.println( Rotation.encode( "My Message", 3 ) ); // outputs "PBPHVVDJH"
 * System.out.println( Rotation.decode( "PBPHVVDJH", 3 ) ); // outputs "MYMESSAGE"
 *
 * You should make every letter uppercase and remove spaces.
 */
public class Rotation
{
    public String encode( String msg, int rot )
    {
        
    }

    public String decode( String msg, int rot )
    {
        
    }
}